<%= link_to "Back to Pictures", pictures_url %> | <%= link_to "Edit", edit_picture_path(@picture) %> | <%= link_to "Destroy", picture_path(@picture), method: :delete, data: {confirm: "Are you sure you want to delete this picture?"}  %>

<p><%= @picture.title %>, <%= @picture.artist %></p>
<%= image_tag @picture.url %>

<h2>Votes</h2>
<p><%= @picture.upvotes %> x upvotes.</p>
<p><%= @picture.downvotes %> x downvotes.</p>
<%= render 'pictures/votes' %>

<h2>Comments</h2>
<%= render @picture.comments %>

<h2>Add a comment:</h2>
<%= render 'comments/form' %>